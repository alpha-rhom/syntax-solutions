<%= form_with(model: user_like, local: true) do |form| %>
  <% if user_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_like.errors.count, "error") %> prohibited this user_like from being saved:</h2>

      <ul>
      <% user_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :library_id %>
    <%= form.number_field :library_id %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
